<%= form_with url: end_foster_request_path, method: :post do |form| %>
    <div class="field">
        <%= form.label :pet_name, "Pet name: #{@pet.name}" %>
    </div>

    <div class="field">
        <%= form.label :comment %>
        <%= form.text_area :comment %>
    </div>

    <div class="field">
        <%= form.button "Submit", data: {confirm: "Are you sure?"} %>
    </div>
<% end %>

<%= link_to "Back", @pet %>