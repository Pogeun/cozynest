<nav class="navbar navbar-dark bg-dark navbar-expand-lg justify-content-between align-items-center">
    <div class="col-4 align-items-center noselect">
        <a class="navbar-brand" href="/">Cozynest</a>
    </div>
    <ul class="navbar-nav col-4 d-flex justify-content-end align-items-center noselect">
        <li class="nav-item">
            <%= link_to "Home", "/", class: "nav-link" %>
        </li>
        <li class="nav-item">
            <%= link_to "Donation", donation_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
            <div class="dropdown show">
                <a class="dropdown-toggle nav-link" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <%= current_user.email %>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                    <%= link_to "Sign out", destroy_user_session_path,  method: :delete, class: "dropdown-item" %>
                </div>
            </div>
        <% else %>
            <%= link_to "Register", new_user_registration_path, class: "nav-link" %>
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
        <% end %>
    </ul>
</nav>