<nav class="navbar navbar-expand-lg navbar-dark bg-dark justify-content-between">
    <div class="col-4 align-items-center">
        <%= link_to "Cozynest", root_url, class: "navbar-brand" %>
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarToggler">
        <ul class="navbar-nav col-4 d-flex justify-content-end">
            <li class="nav-item">
                <%= link_to "Home", root_url, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to "Pets", pets_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
                <%= link_to "Donation", donation_path, class: "nav-link" %>
            </li>
            <% if user_signed_in? %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle">
                        <%= current_user.username %>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right">
                        <%= link_to "Edit account", edit_user_registration_path, class: "dropdown-item" %>
                        <div class="dropdown-divider"></div>
                        <%= link_to "Manage pets", user_pets_path, class: "dropdown-item" %>
                        <% if current_user.shelter? %>
                            <%= link_to "Register pet", new_pet_path, class: "dropdown-item" %>
                        <% end %>
                        <div class="dropdown-divider"></div>
                        <%= link_to "Sign out", destroy_user_session_path, class: "dropdown-item", method: :delete %>
                    </div>
                </li>
            <% else %>
                <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
                <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
            <% end %>
        </ul>
    </div>
</nav>